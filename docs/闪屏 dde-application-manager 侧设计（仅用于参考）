# DDE AM 预启动闪屏（Prelaunch Splash）v2 设计概要（供 Treeland 实现参考）

## 1. 文档目的

本文档用于向 Treeland 侧说明：

- AM（dde-application-manager）当前如何使用 prelaunch splash 协议；
- v2 协议与 v1 的关键差异和行为契约；
- Treeland 需要满足的实现点与边界场景；
- 可执行的联调与验收清单。

> 说明：Treeland 侧无需阅读 AM 源码即可依据本文实现与联调。

---

## 2. 当前状态（结论先行）

- AM 客户端已切换到 **treeland-prelaunch-splash-v2**。
- 协议调用入口是 manager 的 `create_splash(...)`，返回 `treeland_prelaunch_splash_v2` 对象。
- 关闭闪屏不再使用 manager 级 close 方法，而是调用对象级 `destroy()`。
- AM 采用 **instance_id 粒度** 管理闪屏，支持同一 app 多实例并发。

---

## 3. AM 侧关键行为契约（Treeland 必须感知）

### 4.1 创建时机

AM 会在 launch 请求的关键参数校验通过后才申请 splash，避免失败路径遗留闪屏。

### 4.2 唯一关联键

AM 发送以下三元组：

- `app_id`
- `instance_id`
- `sandbox_engine_name`（当前固定为 `dde-application-manager`）

Treeland 应使用该三元组关联闪屏与后续窗口/实例事件。

### 4.3 多实例策略

- 同一 `app_id` 允许多个不同 `instance_id` 的 splash 并存。
- AM 如果发现同一个 `instance_id` 已有 active splash，会跳过重复创建请求。

### 4.4 关闭策略

- 单实例退出：AM 调 `closeSplash(instance_id)`，底层执行该对象 `destroy()`。
- 批量分离/清理：AM 会遍历已跟踪的 instance_id，逐个调用关闭。
- helper 析构：会对剩余对象调用 `destroy()`，然后释放 wrapper。

---

## 4. AM 侧调用链（对 Treeland 最重要）

### 5.1 初始化

仅 Wayland 会话下初始化 `PrelaunchSplashHelper`。

### 5.2 Launch 主流程

1. 解析 desktop entry、exec、参数与兼容层处理。
2. 校验通过后生成 `instanceRandomUUID`。
3. 触发 `helper->show(appId, instanceId, iconName)`。
4. helper 内部调用 `create_splash(...)` 获取对象句柄并缓存。

### 5.3 实例移除主流程

- systemd 报告实例移除后，AM 找到对应 `InstanceService`。
- 提取该实例 `instanceId`。
- 调用 `closeSplashForInstance(instanceId)`。
- helper 取出对应 splash 对象并 `destroy()`。

---

## 5. 图标缓冲区行为（icon_buffer）

AM 通过 wl_shm 构建图标 buffer 并传给 `create_splash`：

- 图标来源：`QIcon::fromTheme(iconName)`；找不到可为空 buffer；
- 尺寸策略：优先 128，失败 fallback 128x128；
- DPR：按 device pixel ratio 分配物理尺寸 buffer；
- buffer 生命周期：直到 compositor 触发 `wl_buffer.release` 才回收。

**Treeland 要求：**

- 必须接受 `icon_buffer = null`（显示默认样式或无图标）；
- 必须正确消费并释放 buffer 引用，避免内存/资源泄漏。

---

## 6. 建议的 Treeland 端状态模型

建议内部状态键：

```text
key = (sandbox_engine_name, app_id, instance_id)
```

建议状态：

- `Pending`：已创建 splash，等待窗口映射；
- `Matched`：窗口已匹配，splash 被关闭；
- `DestroyedByClient`：收到 `destroy()`，主动关闭；
- `TimedOut`（可选）：超时自动关闭并可发事件；
- `OrphanedCleanup`：连接断开或资源回收触发清理。

---

## 7. Treeland 实现要求（必须项）

1. 提供 `treeland_prelaunch_splash_manager_v2` global；
2. 正确处理 `create_splash`，返回独立 `treeland_prelaunch_splash_v2` 资源；
3. 对象 `destroy` 必须立即关闭该对象对应 splash；
4. 支持同 app 多实例并发（按 `instance_id` 区分）；
5. `destroy` 需要幂等（重复/无效对象调用不应导致崩溃）；
6. 客户端断开时自动清理残留 splash；
7. 窗口映射匹配时优先使用三元组精确匹配。

---

## 8. Treeland 可选增强（建议项）

1. 对象级事件扩展（例如 `timed_out` / `closed_by_compositor`）；
2. 兜底超时策略（避免极端场景常驻闪屏）；
3. 诊断日志包含三元组与生命周期状态，便于跨进程联调。

---

## 9. AM 侧可观测日志（联调抓手）

日志分类：`dde.am.prelaunch.splash`

常见关键日志：

- `Show prelaunch splash request <appId> instance <instanceId> icon <iconName>`
- `Sent create_splash for <appId> instance <instanceId> ...`
- `Destroyed splash for instance <instanceId>`
- `Skip prelaunch splash (autostart)`
- `Skip prelaunch splash (singleton with existing instance)`
- `Instance <instanceId> already has an active splash, skipping`

联调建议：对照 Treeland 日志打印相同三元组字段。

---

## 10. 关键边界场景（Treeland 需要通过）

1. **应用启动失败（未生成窗口）**
   - 预期：AM 后续实例清理会触发 `destroy()`，闪屏应消失。

2. **应用崩溃（窗口未映射）**
   - 预期：instance removed 后触发关闭，闪屏不残留。

3. **同 app 多实例并发**
   - 预期：每个 instance_id 一个独立 splash，互不干扰。

4. **重复创建同 instance_id**
   - 预期：AM 会跳过重复申请；Treeland 侧即便收到重复也应稳健处理。

5. **客户端异常退出**
   - 预期：连接断开后 Treeland 回收对应 splash 资源。

6. **空 icon_buffer**
   - 预期：不崩溃，按默认视觉降级显示。

---

## 11. 联调验收清单（建议）

- [ ] Wayland 下 AM 初始化 helper 成功；
- [ ] create_splash 能收到并创建对象；
- [ ] destroy 能立即关闭对应 splash；
- [ ] 非单例应用多开时，多个 splash 并存且可分别关闭；
- [ ] instance remove 与 splash close 一一对应；
- [ ] client crash 不留下孤儿 splash；
- [ ] icon_buffer 有/无两种路径均可正常展示；
- [ ] 关键日志能通过 app_id + instance_id 追踪全链路。

---

## 12. 代码定位（仅供追溯）

- 协议定义：`treeland-protocols/xml/treeland-prelaunch-splash-v2.xml`
- AM Wayland 生成协议绑定：`src/dbus/CMakeLists.txt`
- helper 声明：`src/dbus/prelaunchsplashhelper.h`
- helper 实现：`src/dbus/prelaunchsplashhelper.cpp`
- 启动与实例生命周期：`src/dbus/applicationservice.cpp`
- helper 初始化入口：`src/dbus/applicationmanager1service.cpp`
- systemd unit 解析（含 instanceID）：`src/global.h` 的 `processUnitName(...)`

---

## 13. 给 Treeland AI 的最短提示词（可直接复制）

请按当前 `treeland_prelaunch_splash_v2` 实现 compositor 端：

1. 实现 `treeland_prelaunch_splash_manager_v2.create_splash(new_id, app_id, instance_id, sandbox_engine_name, icon_buffer)`；
2. 使用 `(sandbox_engine_name, app_id, instance_id)` 作为闪屏唯一键；
3. 为每次 create 返回独立 `treeland_prelaunch_splash_v2` 对象；
4. 对象 `destroy()` 需立即关闭该对象对应闪屏并释放资源；
5. 支持同 app 多实例并发；
6. 允许 icon_buffer 为空；
7. 客户端断开时自动回收所有未关闭 splash；
8. 预留对象事件扩展（例如 timeout/closed reason）。

（AM 当前 sandbox_engine_name 固定为 `dde-application-manager`。）
